# ============================================================================
# AI Novel Editor - Requirements
# ============================================================================
# This file lists all dependencies needed to run the AI Novel Editor.
# Install with: pip install -r requirements.txt
# ============================================================================

# Core Dependencies (Required)
# ----------------------------------------------------------------------------
pydantic>=2.0.0,<3.0.0          # Data validation and settings management
python-dotenv>=1.0.0            # Environment variable management

# Web UI (Required)
# ----------------------------------------------------------------------------
streamlit>=1.30.0,<2.0.0        # Web application framework

# Testing (Development)
# ----------------------------------------------------------------------------
pytest>=7.0.0,<8.0.0            # Testing framework

# ============================================================================
# Optional Dependencies (for future features)
# ============================================================================
# Uncomment the sections below when implementing these features:

# Google AI Integration (for LLM-powered editing)
# ----------------------------------------------------------------------------
google-genai>=0.3.0             # Google Gemini API client

# Advanced Visualization
# ----------------------------------------------------------------------------
streamlit-agraph>=0.0.45        # Graph visualization for question tree (mind map)
audio-recorder-streamlit>=0.0.8 # Browser-based audio recording
# plotly>=5.0.0                 # Interactive charts
# networkx>=3.0                 # Graph data structures

# Audio Features (using Gemini's native audio understanding)
# ----------------------------------------------------------------------------
# Audio transcription is handled by google-genai (already included above)
# No additional dependencies needed for audio features!
#
# Optional: For advanced audio processing
# pyaudio>=0.2.14               # Audio recording (if implementing browser recording)
# pydub>=0.25.1                 # Audio format conversion

# Database Backend (for production/multi-user)
# ----------------------------------------------------------------------------
# sqlalchemy>=2.0.0             # SQL toolkit and ORM
# aiosqlite>=0.19.0             # Async SQLite driver
# psycopg2-binary>=2.9.0        # PostgreSQL driver
# alembic>=1.12.0               # Database migrations

# Export Features
# ----------------------------------------------------------------------------
# reportlab>=4.0.0              # PDF generation
# python-docx>=1.0.0            # DOCX generation
# ebooklib>=0.18                # EPUB generation

# Advanced AI Features
# ----------------------------------------------------------------------------
# langchain>=0.1.0              # LLM application framework
# chromadb>=0.4.0               # Vector database for RAG
# sentence-transformers>=2.2.0  # Embeddings for semantic search

# ============================================================================
# Notes:
# ============================================================================
# - The application works with just the core dependencies
# - Optional dependencies can be installed as needed
# - For production deployment, consider pinning exact versions
# - Use 'pip install -r requirements.txt' for basic installation
# - Use 'pip install -r requirements-dev.txt' for development (if created)
# ============================================================================
